export enum Spacing {
  xs = 4,
  s = 8,
  m = 12,
  l = 16,
  xl = 24,
}

export enum BorderThickness {
  thin = 0.5,
  medium = 1,
  thick = 2,
}

export enum BorderRadius {
  xs = 4,
  s = 8,
  m = 12,
  l = 100,
}

export enum BorderStyle {
  solid = 'solid',
  dotted = 'dotted',
  dashed = 'dashed',
}

export type BorderThicknessType = 'thin' | 'medium' | 'thick';
export type BorderRadiusType = 'xs' | 's' | 'm' | 'l';
export type BorderStyleType = 'solid' | 'dotted' | 'dashed';

export type SpacingType = 'xs' | 's' | 'm';
