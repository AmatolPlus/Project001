import React from 'react';
import {Divider as RPDivider} from 'react-native-paper';

import {IDivider} from '../types/Divider';

const Divider = (props: IDivider) => <RPDivider {...props} />;

export default Divider;
